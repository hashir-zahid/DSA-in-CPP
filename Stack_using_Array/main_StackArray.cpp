#include <iostream>
#include "Stack.h"
using namespace std;

void decToBin(int num)

int main(int argc, char** argv)
{
	int arr[5];
	return 0;
}
void decToBin(int num)
{
	Stack<int> s1(arr.length());
	int arr[5];
	while(num!=0)
	{
		s1.push(num%2);
		num=num/2;
	}
	while(!isEmpty())
	{
		cout<<s1.pop();
	}
}
